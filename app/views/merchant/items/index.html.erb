All Merchant's Items

<% @items.each do |item| %>
  <section class="items-<%= item.id %>">
    <%= item.name %>
    <%= item.description %>
    <%= item.price %>
    <%= image_tag(item.image, alt: "image-#{item.name}") %>
    <% if item.active? %>
      active
      <%= button_to "deactivate", "/merchant/items/#{item.id}", method: :delete %>
    <% elsif !item.active? %>
      inactive
    <% end %>
    <%= item.inventory %>
  </section>
<% end %>
