<h1>Your Dashboard</h1>

<h2>Packaged:</h2>
<p><%= "None at this time" if @orders.packaged.empty? %>
<% @orders.packaged.each do |order| %>
  <p>Name: <%= order.name %></p>
  <p>Order Id: <%= order.id%></p>
  <p>Created At: <%= order.created_at %></p>
  <%= link_to "Ship Order", "/admin/dashboard?id=#{order.id}", method: :patch %>
<% end %>

<h2>Pending:</h2>
<p><%= "None at this time" if @orders.pending.empty? %>
<% @orders.pending.each do |order| %>
  <p>Name: <%= order.name %></p>
  <p>Order Id: <%= order.id%></p>
  <p>Created At: <%= order.created_at %></p>
<% end %>

<h2>Shipped:</h2>
<p><%= "None at this time" if @orders.shipped.empty? %>
<% @orders.shipped.each do |order| %>
  <p>Name: <%= order.name %></p>
  <p>Order Id: <%= order.id%></p>
  <p>Created At: <%= order.created_at %></p>
<% end %>

<h2>Cancelled:</h2>
<p><%= "None at this time" if @orders.cancelled.empty? %>
<% @orders.cancelled.each do |order| %>
  <p>Name: <%= order.name %></p>
  <p>Order Id: <%= order.id%></p>
  <p>Created At: <%= order.created_at %></p>
<% end %>
